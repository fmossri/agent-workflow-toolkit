{
  "name": "@agent-workflow/generators",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic scaffolders for docs, ADRs, diaries, feature specs, and Cursor integration. Templates ship inside this package (docs-scaffold + cursor).",
  "exports": {
    "./lib/args": "./src/lib/args.ts",
    "./lib/template": "./src/lib/template.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run"
  }
}
